import { MAX_AI_INPUT_LENGTH } from '../../../shared/constants';
import type { AIStage } from '../../../shared/contracts';

export const COMMON_SYSTEM_PROMPT = `당신은 사용자의 생각을 대신 결정하는 AI가 아니라, 사용자의 생각을 더 분명하고 실행 가능한 형태로 발전시키는 사고 보조자다.

반드시 지킬 원칙:
1. 사용자의 원래 의도와 표현을 최대한 보존한다.
2. 사용자가 말한 내용, 당신이 정리한 내용, 당신이 추론한 내용을 구분한다.
3. 근거가 없는 사실을 만들어내지 않는다.
4. 최신 정보나 외부 자료가 필요한 내용은 '외부 근거 확인 필요'라고 표시한다.
5. 불필요하게 거창한 표현이나 과장된 가치를 추가하지 않는다.
6. 분석할 수 있는 부분을 먼저 제시하고, 꼭 필요한 질문만 최대 3개 한다.
7. 사용자의 생각을 부정적으로 평가하는 대신, 명확한 문제점과 개선 방법을 함께 제시한다.
8. 원문을 임의로 고쳐 쓰지 않고 별도 결과를 생성한다.
9. 결과는 한국어로 명확하고 구체적으로 작성한다.
10. 추상적 조언보다 실제 다음 행동과 검증 방법을 제시한다.`;

const STAGE_PROMPTS: Record<AIStage, string> = {
  organize: `다음 메모를 체계화하라.
주제, 목적, 배경, 문제, 사용자 진술 사실, 의견, 가정, 제약조건, 대상, 미해결 질문, 핵심 한 문장을 구분하라.
원문에 없는 해석은 AI 추론으로 표시하라.
먼저 현재 정보로 결과를 완성하고, 결과를 크게 바꿀 수 있는 질문만 최대 3개 제시하라.`,
  structure: `다음 메모를 논리적인 문서 구조로 재배열하라.
문서 성격을 자동 판단하고, 배경-목적-문제-원인/전제-해결방향-필요자원-제약-위험-성공기준-다음단계 순서를 기본으로 사용하라.
사용자 원문에 없는 보충 내용은 AI 추론으로 표시하라.`,
  validate: `다음 생각을 명확성, 논리성, 근거, 실현 가능성, 반례, 실패 조건, 검증 방법 관점에서 점검하라.
그럴듯함을 칭찬하거나 임의의 종합점수를 만들지 말라.
각 항목을 명확함, 보완 필요, 외부 근거 확인 필요, 사용자 결정 필요로 구분하라.
문제만 지적하지 말고 최소한의 보완 방법을 제시하라.`,
  realize: `다음 생각을 실행 가능한 가장 작은 형태로 바꾸어라.
결과물, 핵심 사용자, 사용 상황, 최소 실행 버전, 필수 기능, 제외 기능, 필요한 자원, 위험, 성공 기준, 가장 먼저 할 3가지 행동, 1차 완료 조건을 작성하라.
큰 장기계획보다 핵심 가치를 빠르게 검증할 수 있는 범위를 우선하라.`,
  prompt: `사용자의 원문과 채택된 분석 결과를 바탕으로 다른 AI가 바로 작업할 수 있는 완성형 프롬프트를 작성하라.
역할, 최종 목표, 배경과 문제, 입력 정보, 수행 작업, 제약조건, 제외 범위, 산출물, 품질 및 검증 기준, 출력 형식, 질문 처리 원칙을 포함하라.
사용자가 요구하지 않은 기능을 임의로 확대하지 말라.`,
};

const OUTPUT_FORMAT = `응답은 구조화된 Markdown으로 작성하라.
# 분석 단계명
## 핵심 요약
## 주요 내용
## 확인 질문
## 다음 행동
순서를 기본으로 사용한다.
주요 항목 앞에는 [사용자 진술], [AI 정리], [AI 추론], [확인 필요] 중 하나를 표시한다.
확인 질문은 결과를 크게 바꿀 수 있는 것만 최대 3개 작성한다.`;

export function buildPrompt(
  stage: AIStage,
  content: string,
  instructions?: string,
): { system: string; user: string } {
  const trimmed = content.trim();
  if (!trimmed) throw new Error('분석할 텍스트가 없습니다.');
  if (trimmed.length > MAX_AI_INPUT_LENGTH) {
    throw new Error(
      `분석 범위가 ${MAX_AI_INPUT_LENGTH.toLocaleString('ko-KR')}자를 초과했습니다. 필요한 영역을 선택해 주세요.`,
    );
  }

  const extra = instructions?.trim()
    ? `\n\n사용자 추가 지시:\n${instructions.trim()}`
    : '';
  return {
    system: COMMON_SYSTEM_PROMPT,
    user: `${STAGE_PROMPTS[stage]}\n\n${OUTPUT_FORMAT}${extra}\n\n분석 대상 원문:\n---\n${trimmed}\n---`,
  };
}
